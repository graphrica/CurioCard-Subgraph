type CardType @entity {
  id: ID!
  name: String! 
  supply: BigInt! 
  ipfsHash: String! 
  symbol: String!
  description: String!
  wrapped: Bytes
}

type Card @entity {
  id: ID!
  type: CardType!
  owner: Bytes! # address
  isWrapped: Boolean!
}
